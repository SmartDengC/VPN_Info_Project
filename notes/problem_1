1：不知道怎么设计URL,无法更好的使用url来传资源？

    本任务是实现数据库内容的增删改查，根据使用的技术，
    只有两个url，users和user，
    一个用来查询全部用户信息，另个实现查询某个用户信息，
    将增删改，融合到了users中。

2：搞不清楚如何实现 post ，只能用get 方法来调用 post？

    post的数据在http请求体中，
    首先应该明白 前端提交信息-->http请求--->后台处理，
    我们只是直接从http请求入手，所以只要从请求体中湖区数据即可。

3：数据库关闭，使用python中的pymysql库 ，没有找到关闭数据库的方法？

    使用DBUtils 中的相关方法
4：现在还没有能力 根据 webservice 规则 上线

    本需求不使用这项技能
5:测试 unittest？

    未完任务1（代码中请求的是请求体中的内容，如何测试其功能？）
6:防止sql注入?

    1：url 中传过来的参数是str类型的

7：在此理解数据库 操作。


时间 2018-20-21
8:python 如何像java中一样创建一个只有属性的类？
